server:
  port: ${BACKEND_PORT:18080}

spring:
  application:
    name: smart-seat-reservation
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:smart_seat}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: ${MYSQL_USER:smart_seat}
    password: ${MYSQL_PASSWORD:smart_seat_dev}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

springdoc:
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info

smart-seat:
  reservation-expiration:
    fixed-delay-ms: ${RESERVATION_EXPIRATION_DELAY_MS:60000}
  wifi-presence:
    fixed-delay-ms: ${WIFI_PRESENCE_RELEASE_DELAY_MS:60000}
  reservation-rules:
    checkin-grace-minutes: ${RESERVATION_CHECKIN_GRACE_MINUTES:10}
    checkin-lead-minutes: ${RESERVATION_CHECKIN_LEAD_MINUTES:10}
    max-advance-days: ${RESERVATION_MAX_ADVANCE_DAYS:7}
    reservation-open-hour: ${RESERVATION_OPEN_HOUR:18}
    daily-active-reservation-limit: ${RESERVATION_DAILY_ACTIVE_LIMIT:3}
    wifi-offline-release-minutes: ${RESERVATION_WIFI_OFFLINE_RELEASE_MINUTES:15}
    seat-lock-minutes: ${RESERVATION_SEAT_LOCK_MINUTES:60}
  seat-lock:
    fixed-delay-ms: ${SEAT_LOCK_RELEASE_DELAY_MS:60000}
